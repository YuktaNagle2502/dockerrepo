first_name = "yukta"
last_name = "nagle"
full_name = first_name + " " + last_name
print(full_name)
# string can only concatenate with string, we cannot concatenate string with integer
# print(first_name + 3) # this will give error because we cannot concatenate string and integer
print(first_name * 3) # this will print the string 3 times
print(first_name + str(3)) # this will convert the integer to string and then concatenate
print(first_name + "4")